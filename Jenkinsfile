pipeline {
    agent any 
    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/vyphotphet100/nestjs-test-jenkins.git'
            }
        }
    }
}